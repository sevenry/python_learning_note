import logRegres
from numpy import *

#dataArr, labelMat = logRegres.loadDataSet()##得到的是list
#print(labelMat)
#cc=logRegres.gradAscent(dataArr, labelMat)
#print(cc)
#logRegres.plotBestFit(cc)

#weights=logRegres.stocGradAscent1(array(dataArr),labelMat)#和cc相比，第一个参数不是list，而是array
#print(weights)
#logRegres.plotBestFit(weights)

logRegres.multiTest()
##关于

